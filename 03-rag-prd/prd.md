# AI PRD · Juno

_Module 3. Generated by `M3 - AI PRD Builder.html`. Paste over `03-harness-prd/prd.md`._

## Problem & user

RocketShip PMs need this handled: Juno reads the strategy and the customer signal, ranks the backlog, and defends every call with a citation, inside limits a human set on purpose. Juno does this inside limits a human set on purpose, and defends every call it makes.

## The harness

| Surface | Specification |
|---|---|
| 01 Context | 1. RocketShip Q3 2026 Strategy One-Pager - the single authority |
| 02 Tools | 1. search_strategy (Read)  2. read_tickets (Read) 3. draft_priority (Draft) 4. write_roadmap (Write) |
| 03 Loop | 1) Juno runs a plan / act / observe loop, maximum 5 turns per request. |
| 04 Memory | Per PM, per sprint. Juno remembers the rational behind each ranking it produced this sprint so it can explain a change |
| 05 Permissions | read auto · draft auto · write confirm · send blocked |
| 06 Verification | _(not yet specified)_ |

## 01 Context · Data Requirements

**Required sources, and exclusions**

1. RocketShip Q3 2026 Strategy One-Pager - the single authority

**How fresh must it be**

Real-time trigger - Juno must refresh itself when a new JIRA/Slack ticket is received 
Unreachable source - Juno proceeds but banners "JIRA tickets/Slack unavailable - evidence incomplete" must be displayed on every card.

## 02 Tools · System Capabilities

**The verb list**

1. search_strategy (Read) 
2. read_tickets (Read)
3. draft_priority (Draft)
4. write_roadmap (Write)

**Deliberate omissions**

send_message/post_to-slack/send_email (class SEND) - Changes state outside Juno (someone's inbox or channel) and can't be undone by reloading the page
post_customer_update (class SEND) - Customer-facing text needs a human sign-off step Juno doesn't model yet
delete_ticket/archive_ticket don't exist (class DESTRUCTIVE) - Irreversible, and prioritization never requires mutating a ticket. Juno only ever reads tickets, never writes to them

## 03 Loop · AI Costs & Latency

**Turn ceiling and escalation**

1) Juno runs a plan / act / observe loop, maximum 5 turns per request. 
2) On turn 5, Juno must return its best current answer, clearly labelled "turn limit reached", instead of continuing or silently truncating. 
3. If the same tool fails 3 times consecutively, stop the loop immediately and
escalate: return a human-readable summary of what it was trying to do, what
failed, and what a human should check. Never retry a fourth time.
4. Track and display cumulative token cost per request in the tool trace footer.
5. Target p95 under 8 seconds end to end. Log any request that exceeds it.

**Latency and cost target**

answers back in p95 < 8 seconds almost every time. Anything over 8 seconds triggers console.warn, gets its own entry in the tool trace, and the footer's RUNNING P95 figure turns red in the trace tool

## 04 Memory · Data Requirements

**What persists, at what scope**

Per PM, per sprint. Juno remembers the rational behind each ranking it produced this sprint so it can explain a change

**Expiry and write rules**

Fact should remain alive. If there is a different between Juno's output and human correction, human correction will outrank,

## 05 Permissions · AI Risks & Mitigations

**The permission tier**

| Side-effect class | Tier |
|---|---|
| read | auto |
| draft | auto |
| write | confirm |
| send | blocked |

**Justification**

strategy READ -> AUTO runs without asking
read
_
tickets READ -> AUTO runs without asking
draft
_priority DRAFT -> AUTO runs without asking
write
_
roadmap WRITE -> CONFIRM must not execute until a human clicks

## 06 Verification · AI Testing & Measurement

**The check before output ships**

_(not yet specified)_

**Failure behaviour**

_(not yet specified)_

## Eval plan

_Stub. Module 6 fills this in: golden set, pass thresholds, and regression cadence for the verification gate above._

## Out of scope

Any capability not in the verb list above, and any action tiered `blocked`. Both are decisions on the record, not omissions.

_____
